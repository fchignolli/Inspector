<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.example.inspector">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:name=".Utils.App"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".Controller.ForgotPassowordActivity"
                android:parentActivityName=".Controller.LoginActivity"/>
        <activity
                android:name=".Controller.RegisterActivity"
                android:parentActivityName=".Controller.LoginActivity" />
        <activity android:name=".Controller.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                        android:host="dssad.cin"
                        android:pathPattern="/saas"
                        android:scheme="http" />
            </intent-filter>
        </activity>
        <activity
                android:name=".Controller.LoginActivity"
                android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>